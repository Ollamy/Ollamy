import styled from 'styled-components';

// eslint-disable-next-line
interface LessonTableProps {}

const LessonTable = ({}: LessonTableProps) => {
  return <Container></Container>;
};

const Container = styled.div``;

export default LessonTable;
