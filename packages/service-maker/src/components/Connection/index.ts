import styled from 'styled-components';

const Image = styled.img`
  width: auto;
  height: auto;
`;

const Title = styled.h1`
  margin-top: 140px;
  margin-bottom: 60px;

  color: #e6674f;
  font-size: 40px;
  font-weight: bolder;
`;

const Label = styled.label``;

const Text = styled.p``;

const ImportantText = styled.span`
  color: #876bf6;

  cursor: pointer;
`;

export { Image, Title, Label, Text, ImportantText };
